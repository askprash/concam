# Ralph session progress notes for continuity
# session: v0.1 — initial scaffolding (2026-04-10)

## [2026-04-10] Scaffolding — DONE (commit 5bad2c0)

**Task completed:** Initialize the mit-concam-pipeline project with UV, package layout, YAML config, and SLURM template.

**Files changed:**
- `pyproject.toml` — UV project config; deps: click, pyyaml, numpy, opencv-python-headless, duckdb, scipy; entry point `concam = concam.cli:main`; requires-python >=3.11
- `concam/__init__.py` — package root
- `concam/cli.py` — Click group with `run` (--date, --output-dir, --from-stage, --dry-run), `bundle`, and `ingest-labels` commands (later stages raise ClickException as stubs)
- `concam/config.py` — dataclass hierarchy (SiteConfig, VideoConfig, OcrConfig, AdsbConfig, CalibrationConfig, DetectionConfig, AggregationConfig) + `load_config(path) -> SiteConfig`
- `concam/{ocr,adsb,projection,detection,aggregation,storage}/__init__.py` — placeholder submodule stubs
- `configs/mit_green_building.yaml` — full site config for MIT Green Building camera (GPS 42.360444/-71.089238/84.23m, OCR region [80,875] top-right, ADS-B min 8km/50km radius, calibration npz path, Hough/Canny defaults)
- `slurm/run_pipeline.sh` — sbatch script; accepts YYYY-MM-DD arg; runs `uv run concam run --date $DATE`
- `.gitignore` — standard Python + project-specific ignores

**Key decisions:**
- `requires-python = ">=3.11"` (not 3.14 which uv defaulted to) — cluster likely doesn't have 3.14
- Used `opencv-python-headless` not `opencv-python` — no display server on SLURM nodes
- `pointpicker_calibration_estimate.npz` chosen as the calibration file (has GPS-anchored extrinsics + rotation; geocalib_calibration_estimate.npz lacks extrinsics)
- Stub CLI commands raise `ClickException` rather than `NotImplementedError` so they still print nicely

**Verified:**
- `uv sync` clean (55 packages resolved)
- `uv run concam --help` prints usage
- `uv run concam run --date 2026-04-08 --dry-run` prints 6-stage execution plan
- `load_config('configs/mit_green_building.yaml')` round-trips cleanly

**Next step:** OCR validation notebook (prd.json item 2) — compare fixed-format template reader vs EasyOCR on real April 8 timelapse frames. This is the first go/no-go gate before wiring OCR into the pipeline. The daily timelapse for April 8 is at `/net/d16/data/contrail-camera/2026_04_08_0000_2359.mp4`.
